# 专业名词——中英文对照

## 使用说明

- **你的总结**：来自原文件 `涉及到的专业名词.txt`。
- **补充词汇**：为了方便复习，由我补充的常见 C++ 或本项目相关词汇。
- 英文优先使用写代码和答辩时最常见、最容易说出口的表达。
- 原文中的 `piblic` 已改成正确拼写 `public`。
- “中括号”和“方括号”通常都表示 `[]`，这里保留两个原词并分别说明。

---

## 一、类、对象与面向对象

### 你的总结

- 成员变量 — member variable / data member
- 结构体 — structure / struct
  - `struct` 的成员默认是公开的 — members of a `struct` are `public` by default
- 类 — class
  - `class` 的成员默认是私有的 — members of a `class` are `private` by default
- 析构函数 — destructor
- 继承 — inheritance
- 多态 — polymorphism
- 虚函数 — virtual function
- 纯虚函数 — pure virtual function

### 补充词汇

- 对象 — object
- 实例 — instance
- 构造函数 — constructor
- 基类 / 父类 — base class / parent class
- 派生类 / 子类 — derived class / child class
- 抽象类 — abstract class
- 封装 — encapsulation
- 访问权限 — access control / access level
- 公开的 — public
- 私有的 — private
- 受保护的 — protected
- 重写虚函数 — override a virtual function
- `override` 关键字 — `override` specifier
- `final` 关键字 — `final` specifier
- 成员函数 — member function

---

## 二、函数、参数与 C++ 语言结构

### 你的总结

- 模板参数 — template parameter
- 强制转换 — type cast / explicit cast
- 引用 — reference
- 初始化 — initialization
- 函数体 — function body
- 参数 — parameter / argument
- 形参 — parameter / formal parameter
- 实参 — argument / actual argument
- 返回值类型 — return type
- `const`：只读，不允许修改 — `const`: read-only / cannot be modified
- `&` 表示引用 — `&` means reference
  - 不使用引用时，函数调用通常需要复制对象 — without a reference, the object is usually copied
- `std`：C++ 标准库的命名空间 — `std`: the C++ standard library namespace
- Lambda：临时使用的匿名函数 — lambda: a temporary anonymous function
- 语句结构体（原文）— statement / structure
  - 这不是常见的固定术语。如果原意是两个词，可拆成“语句”和“结构体”。
  - This is not a common fixed term. It can be split into “statement” and “structure”.

### 补充词汇

- 函数 — function
- 函数声明 — function declaration
- 函数定义 — function definition
- 调用函数 — call a function / function call
- 返回 — return
- 默认参数 — default argument
- 模板实参 — template argument
- 按值传递 — pass by value
- 按引用传递 — pass by reference
- 函数重载 — function overloading
- 作用域 — scope
- 命名空间 — namespace
- 匿名命名空间 — anonymous namespace
- 关键字 — keyword
- 声明 — declaration
- 定义 — definition
- 语句 — statement
- 代码块 — code block
- 头文件 — header file
- 源文件 — source file
- `#include` 指令 — `#include` directive
- 编译时常量 — compile-time constant
- `constexpr` — compile-time constant expression
- 类型推导 — type deduction
- `auto` — automatic type deduction
- 显式转换 — explicit conversion
- `static_cast` — `static_cast`

---

## 三、指针、地址与内存

### 你的总结

- 取地址 — take the address / address-of
- 内存地址 — memory address
- 智能指针 — smart pointer
- 指针 — pointer
- 指向 — point to

### 补充词汇

- 地址运算符 `&` — address-of operator `&`
- 解引用 — dereference
- 解引用运算符 `*` — dereference operator `*`
- 空指针 — null pointer
- `nullptr` — null pointer literal
- 普通指针 / 原始指针 — raw pointer
- 独占智能指针 — `unique_ptr`
- 共享智能指针 — `shared_ptr`
- 创建独占智能指针 — `make_unique`
- 所有权 — ownership
- 转移所有权 — transfer ownership
- 移动语义 — move semantics
- `std::move` — convert to a movable value / `std::move`
- 拷贝 — copy
- 栈内存 — stack memory
- 堆内存 — heap memory
- 动态分配 — dynamic allocation
- 释放内存 — free memory / release memory
- 内存泄漏 — memory leak
- 悬空指针 — dangling pointer
- 对象生命周期 — object lifetime
- 资源管理 — resource management

---

## 四、判断、表达式、运算符与符号

### 你的总结

- 判断 — check / condition check
- 判断条件 — condition
- 表达式 — expression
- 短路求值 — short-circuit evaluation
- 短路特性 — short-circuit behavior
- 三目运算符 — ternary operator / conditional operator
- `&&` 怎么念 — logical AND / “and” / “double ampersand”
- 取余运算符 `%` — modulo operator / remainder operator
- 奇数 — odd number
- 偶数 — even number
- `++` 在前，先加再用 — prefix increment: increment first, then use
- `++` 在后，先用再加 — postfix increment: use first, then increment
- 分号 `;` — semicolon
- 逗号 `,` — comma
- 中括号 `[]` — square brackets
- 小括号 `()` — parentheses / round brackets
- 方括号 `[]` — square brackets
- `-` 这个杠 — hyphen / minus sign / dash

### 补充词汇

- 赋值运算符 `=` — assignment operator
- 等于 `==` — equal to / equality operator
- 不等于 `!=` — not equal to / inequality operator
- 大于 `>` — greater than
- 小于 `<` — less than
- 大于等于 `>=` — greater than or equal to
- 小于等于 `<=` — less than or equal to
- 逻辑与 `&&` — logical AND
- 逻辑或 `||` — logical OR
- 逻辑非 `!` — logical NOT
- 加法运算符 `+` — addition operator / plus
- 减法运算符 `-` — subtraction operator / minus
- 乘法运算符 `*` — multiplication operator
- 除法运算符 `/` — division operator
- 自增运算符 `++` — increment operator
- 自减运算符 `--` — decrement operator
- 运算符优先级 — operator precedence
- 布尔值 — Boolean value / bool
- 真 — true
- 假 — false
- 花括号 `{}` — braces / curly brackets
- 尖括号 `<>` — angle brackets
- 冒号 `:` — colon
- 双冒号 `::` — scope resolution operator
- 箭头运算符 `->` — arrow operator
- 点运算符 `.` — dot operator / member access operator

---

## 五、容器、数据与边界

### 你的总结

- 索引 / 下标 — index
- 避免越界 — avoid out-of-bounds access

### 补充词汇

- 数组 — array
- 固定长度数组 — fixed-size array
- 动态数组 — dynamic array
- `std::array` — standard fixed-size array
- `std::vector` — standard dynamic array / vector
- 字符串 — string
- `std::string` — standard string
- 可选值 — optional value
- `std::optional` — optional value
- 空的可选值 — `std::nullopt`
- 队列 — queue
- `std::queue` — standard queue
- 枚举 — enumeration / enum
- 元素 — element
- 第一个元素 — first element
- 最后一个元素 — last element
- 容器大小 — container size
- 迭代器 — iterator
- 起始位置 — beginning / `begin()`
- 结束位置之后 — past the end / `end()`
- 添加到末尾 — append / `push_back()`
- 清空 — clear / reset
- 是否为空 — empty / `empty()`
- 范围 `for` 循环 — range-based `for` loop
- 数组越界 — array index out of bounds

---

## 六、编译、运行、错误、测试与日志

### 你的总结

- 解析 — parse / parsing
- 编译 — compile / compilation
- 命令台输入 / 控制台输入 — terminal input / console input
- 直接报错 — report an error immediately / fail immediately
- 日志 — log / logging
- 连接 — connect / link
  - 普通对象之间使用 `connect`；编译目标文件时使用 `link`。
  - Use `connect` for objects, and `link` for compiled object files.

### 补充词汇

- 编译器 — compiler
- 链接器 — linker
- 构建项目 — build the project
- 编译错误 — compile error
- 链接错误 — linker error
- 运行时错误 — runtime error
- 异常 — exception
- 抛出异常 — throw an exception
- 捕获异常 — catch an exception
- `try` 代码块 — `try` block
- `catch` 代码块 — `catch` block
- 错误信息 — error message
- 警告 — warning
- 调试 — debugging
- 调试器 — debugger
- 断点 — breakpoint
- 日志级别 — log level
- 信息日志 — info log
- 错误日志 — error log
- 测试 — test
- 单元测试 — unit test
- 测试用例 — test case
- 断言 — assertion
- 可执行文件 — executable file
- 命令行参数 — command-line argument
- 返回码 / 退出码 — return code / exit code

---

## 七、GUI、颜色、数学与几何

### 你的总结

- 向上取整 — round up / ceiling
- 透明度 — opacity / transparency
- 交点公式 — intersection formula
- 垂直同步 — vertical synchronization / VSync

### 补充词汇

- 图形用户界面 — graphical user interface / GUI
- 窗口 — window
- 调整窗口大小 — resize the window
- 屏幕宽度 — screen width
- 屏幕高度 — screen height
- 屏幕坐标 — screen coordinate
- 棋盘坐标 — board coordinate
- 坐标点 — coordinate point
- 横坐标 — x-coordinate
- 纵坐标 — y-coordinate
- 网格 — grid
- 网格间距 — grid spacing
- 像素 — pixel
- 帧 — frame
- 帧率 — frame rate
- 每秒帧数 — frames per second / FPS
- 绘制 — draw / render
- 渲染 — rendering
- 背景 — background
- 边界 / 边框 — boundary / border
- 线宽 — line thickness / line width
- 半径 — radius
- 圆心 — center of a circle
- 左上角 — top-left corner
- 右上角 — top-right corner
- 左下角 — bottom-left corner
- 右下角 — bottom-right corner
- 居中 — center / centered
- 缩放 — scale / scaling
- 比例 — ratio / fraction
- 限制数值范围 — clamp a value
- `std::clamp` — clamp a value to a range
- 最小值 — minimum value
- 最大值 — maximum value
- 二维向量 — two-dimensional vector / 2D vector
- 向量长度 — vector length
- 距离 — distance
- 直线 — line
- 线段 — line segment
- 交点 — intersection point
- 平行 — parallel
- 四边形 — quadrilateral / quad
- 三角形 — triangle
- 红绿蓝颜色 — RGB color
- 颜色通道 — color channel
- Alpha 通道 — alpha channel
- 完全不透明 — fully opaque
- 半透明 — semi-transparent

---

## 几个容易混淆的词

- 参数 — parameter / argument
  - 在函数定义中写出来的是 **parameter（形参）**。
  - 调用函数时实际传进去的是 **argument（实参）**。
- 引用 — reference
  - `Type& name` 中的 `&` 通常表示引用。
- 取地址 — address-of
  - `&variable` 中的 `&` 表示取得变量的内存地址。
- 连接 — connect / link
  - 棋子或桥之间“连接”通常说 `connect`。
  - 编译过程中的“链接”通常说 `link`。
- 向量 — vector
  - `std::vector` 是 C++ 动态数组。
  - GUI 几何中的 2D vector 是带 x、y 的方向或移动距离。
