# 问答
*Questions and Answers*

## 我选择的兴趣点
*My chosen point of interest*

**窗口拉大或拉小时，怎么让棋盘一直正常显示**
***How to keep the board displayed correctly when the window is resized***

这部分主要在下面几个函数中：
*This part is mainly in the following functions:*

- `calculate_board_geometry()`：计算棋盘应该放在哪里、画多大；
- *`calculate_board_geometry()`: calculates the board position and size;*
- `board_coordinate_to_screen()`：把棋盘坐标换成屏幕上的位置；
- *`board_coordinate_to_screen()`: changes a board coordinate to a screen position;*
- `make_geometry()`：读取当前窗口大小；
- *`make_geometry()`: reads the current window size;*
- `draw_frame()`：按照算好的位置画棋盘、棋子和桥。
- *`draw_frame()`: draws the board, pegs and bridges at the calculated positions.*

## 一开始可以这样回答
*You can answer like this at the beginning*

问：在你自己做项目的过程中，有什么是你觉得有意思或者感兴趣的地方？
*Q: What did you find interesting while working on the project?*

答：我比较感兴趣的是，窗口拉大或者拉小以后，怎么让棋盘还是正常显示。
*A: I was interested in how to keep the board displayed correctly after resizing the window.*
因为如果直接把棋盘的位置和大小写死，窗口变小以后，棋盘可能会跑到窗口外面；
*If the position and size are fixed, the board may go outside a small window;*
窗口变大以后，棋盘又可能只缩在一个角落里。
*and in a large window, it may stay very small in one corner.*

所以我做的这部分会先看现在的窗口有多大，再计算棋盘应该画多大、放在哪里，
*So my part first checks the current window size and then calculates the board size and position.*
最后让棋盘尽量保持在中间。棋盘上的格子、棋子和桥也会跟着一起调整。
*It keeps the board near the center, and the grid, pegs and bridges adjust with it.*
我们的棋盘还可以旋转，所以旋转以后，外面的边框也要画到正确的位置。
*Our board can also rotate, so its outer border must stay in the correct position.*

我觉得这部分有意思，是因为它不是简单地画一张固定图片，
*I found this interesting because it is not just drawing one fixed picture.*
而是窗口发生变化以后，画面也能自己跟着变化。
*The whole display can adjust itself when the window changes.*

## 教授可能继续问的问题
*Questions the professor may ask next*

### 1

问：为什么不能直接把棋盘的位置写死？
*Q: Why can you not use a fixed position for the board?*

答：因为窗口可以拉大和拉小。位置写死以后，小窗口可能装不下棋盘，
*A: Because the window can be resized. A small window may not have enough room for a fixed board,*
大窗口里棋盘又可能显得太小，所以需要根据窗口大小重新计算。
*and the board may look too small in a large window, so it must be recalculated.*

### 2

问：程序怎么知道窗口现在有多大？
*Q: How does the program know the current window size?*

答：程序会调用 `GetScreenWidth()` 和 `GetScreenHeight()`，
*A: It calls `GetScreenWidth()` and `GetScreenHeight()`.*
分别得到窗口现在的宽度和高度。
*They return the current width and height of the window.*

### 3

问：窗口大小是在什么时候读取的？
*Q: When does the program read the window size?*

答：画每一帧之前都会重新读取。这样用户刚刚调整了窗口，
*A: It reads it again before drawing every frame. If the user resizes the window,*
下一帧就可以按照新的大小重新画，不需要重新启动游戏。
*the next frame can use the new size without restarting the game.*

### 4

问：你是怎么让棋盘放在窗口中间的？
*Q: How do you place the board in the center of the window?*

答：我先计算棋盘画完以后还剩多少空间，再把剩余空间平均分到两边。
*A: I calculate how much space is left and divide that space equally between both sides.*
比如横向多出 100 像素，就在左边和右边各留 50 像素。
*For example, if 100 pixels are left horizontally, each side gets 50 pixels.*

### 5

问：为什么还要给窗口四周留一些空白？
*Q: Why do you leave some empty space around the board?*

答：如果棋盘紧贴窗口边缘，看起来会很挤，而且上方还要显示玩家和回合信息。
*A: A board touching the edges looks crowded. The top also needs game information.*
所以代码会预留一些空间，再把棋盘放进剩下的区域。
*So the code keeps some space and places the board in the remaining area.*

### 6

问：为什么代码里经常出现 `width - 1`？
*Q: Why does `width - 1` often appear in the code?*

答：因为坐标点的数量和中间空格的数量不一样。
*A: Because the number of points and the number of gaps are different.*
例如一排有 12 个点，但是相邻点之间只有 11 个空格，所以要减 1。
*For example, 12 points have only 11 gaps between them, so we subtract 1.*

### 7

问：怎样保证横着的一格和竖着的一格一样大？
*Q: How do you keep horizontal and vertical grid spaces the same size?*

答：程序先分别算出横向和纵向最多能用多大的格子，然后选择较小的那个。
*A: The program calculates both possible sizes and chooses the smaller one.*
横向和纵向都使用这个大小，所以格子不会一边很宽、一边很窄。
*Both directions use that size, so one side is not wider than the other.*

### 8

问：为什么要选择较小的格子大小？
*Q: Why do you choose the smaller grid size?*

答：因为较小的那个一定能让棋盘的宽和高都放进窗口。
*A: Because the smaller size makes both the width and height fit inside the window.*
如果选择较大的结果，棋盘可能在另一个方向超出窗口。
*If we choose the larger one, the board may go outside the window in the other direction.*

### 9

问：棋盘坐标和屏幕坐标有什么区别？
*Q: What is the difference between board coordinates and screen coordinates?*

答：棋盘坐标是游戏里的位置，例如 `(3, 5)`；屏幕坐标是窗口里的像素位置，
*A: A board coordinate is a game position like `(3, 5)`. A screen coordinate is a pixel position,*
例如距离窗口左边多少像素、距离上边多少像素。绘图前需要把前者换成后者。
*such as the distance from the left and top edges. We convert the first one before drawing.*

### 10

问：棋盘坐标具体是怎么换成屏幕位置的？
*Q: How exactly do you change a board coordinate into a screen position?*

答：先找到棋盘左上角，再根据 x 向右移动几格，根据 y 向下移动几格。
*A: Start at the top-left corner, move x grid spaces to the right and y grid spaces down.*
最后到达的位置，就是这个棋盘坐标在屏幕上应该画的位置。
*The final point is where that board coordinate should be drawn on the screen.*

### 11

问：`x_step` 和 `y_step` 是什么意思？
*Q: What do `x_step` and `y_step` mean?*

答：`x_step` 表示 x 增加 1 时，要在屏幕上移动多远；
*A: `x_step` says how far to move on the screen when x increases by 1.*
`y_step` 表示 y 增加 1 时，要移动多远。可以把它们理解成每一格的距离。
*`y_step` does the same for y. You can think of them as one grid-space distance.*

### 12

问：为什么这里用了很多 `float`？
*Q: Why does this part use many `float` values?*

答：因为棋盘居中或者缩放以后，位置不一定正好是整数。
*A: Because a centered or resized position is not always a whole number.*
保留小数可以让线条和棋子的位置更准确，不会因为太早取整而发生偏移。
*Keeping decimals gives more accurate line and peg positions and avoids early rounding errors.*

### 13

问：棋盘旋转以后，代码主要改变了什么？
*Q: What mainly changes in the code when the board rotates?*

答：主要改变的是棋盘外边框四个角的位置。
*A: The four corner positions of the outer board border mainly change.*
棋盘里面 `(x, y)` 这些固定坐标点仍然可以使用同一套方法找到。
*The fixed `(x, y)` points inside can still be found in the same way.*

### 14

问：为什么要分别保存网格的左上角和实际棋盘边框的左上角？
*Q: Why do you save both the grid's top-left and the real board border's top-left?*

答：因为它们不一定是同一个位置。网格的左上角用来计算棋子和桥的位置，
*A: Because they are not always the same point. The grid's top-left is used for pegs and bridges,*
实际边框的左上角用来画旋转后的外轮廓，所以两个都需要保存。
*and the border's top-left is used for the rotated outline, so both are needed.*

### 15

问：窗口变得很小时，棋子和桥会不会看不见？
*Q: Will the pegs and bridges disappear when the window becomes very small?*

答：代码给棋子大小和桥的宽度设置了最小值。
*A: The code sets a minimum peg size and bridge width.*
它们会跟着窗口缩小，但缩到一定程度以后就不会继续变小。
*They shrink with the window, but stop shrinking after reaching that minimum.*

### 16

问：如果棋盘很大，坐标数字挤在一起怎么办？
*Q: What happens when coordinate numbers are too crowded on a large board?*

答：程序不会把每一个数字都一直显示出来，而是根据格子之间的距离跳着显示。
*A: The program does not always show every number. It skips some based on the grid spacing.*
不过第一个坐标和最后一个坐标一定会保留，鼠标移过去还能看到准确坐标。
*The first and last values always stay, and the mouse tooltip shows the exact coordinate.*

### 17

问：为什么画网格线要使用两个循环？
*Q: Why do you use two loops to draw the grid?*

答：第一个循环画所有竖线，第二个循环画所有横线。
*A: The first loop draws all vertical lines, and the second draws all horizontal lines.*
两个方向的线都画完以后，才会组成完整的网格。
*The two sets of lines together make the complete grid.*

### 18

问：为什么把位置计算放在 `gui_logic.cpp`，不全部写进绘图代码？
*Q: Why put the position calculation in `gui_logic.cpp` instead of the drawing code?*

答：这样位置计算和真正画图是分开的。即使不开窗口，
*A: This keeps position calculation separate from actual drawing. Even without opening a window,*
也可以用测试代码检查棋盘的位置、大小和坐标计算是不是正确。
*tests can check whether the board position, size and coordinates are correct.*

### 19

问：你怎么确认这部分计算是正确的？
*Q: How did you check that these calculations are correct?*

答：我们写了测试，例如检查没有旋转时边框是不是横平竖直，
*A: We wrote tests, for example checking whether the border is straight without rotation,*
旋转以后固定坐标点有没有乱跑，以及长方形棋盘的每格大小是不是一样。
*whether fixed points stay after rotation, and whether all grid spaces have equal size.*

### 20

问：你在这部分最需要注意的是什么？
*Q: What did you need to pay the most attention to in this part?*

答：最需要注意的是，不能只让棋盘在默认窗口中看起来正常。
*A: The most important thing was not to make the board work only in the default window.*
还要考虑窗口缩放、不同棋盘大小和旋转角度，不然换一个条件就可能画错。
*I also had to consider resizing, different board sizes and rotation angles.*

## 实在紧张时，只记住这条主线
*If you are nervous, just remember this main idea*

> 先看窗口有多大，再算棋盘能画多大，然后把它放到中间。
> First check the window size, then calculate the board size and place it in the center.
> 棋盘上的每个位置，都从左上角开始，按照 x 和 y 移动对应的格数。
> For each board position, start at the top-left and move the needed x and y grid spaces.

最简单的公式可以记成：
*The simplest formula to remember is:*

```text
屏幕位置 = 棋盘左上角 + x 方向移动的距离 + y 方向移动的距离
Screen position = board top-left + x movement + y movement
```
